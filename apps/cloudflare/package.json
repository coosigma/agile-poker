{
	"name": "@agile-poker/cloudflare",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy",
		"build": "wrangler deploy --dry-run --outdir .wrangler/tmp",
		"typecheck": "tsc --noEmit",
		"test": "vitest run"
	},
	"dependencies": {
		"@agile-poker/app-core": "workspace:*",
		"effect": "^3.21.4"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^4.20260630.0",
		"typescript": "^6.0.3",
		"vitest": "^4.1.9",
		"wrangler": "^4.0.0"
	}
}
